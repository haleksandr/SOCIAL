let initialState = {
    sitebar: [
        {id: 1, name: 'ALEKSANDR'},
        {id: 2, name: 'POL'},
        {id: 3, name: 'AARON'}
    ]
};

const menuReducer = (state = initialState, action) => {
    return state;
};

export default menuReducer;